<Page
    x:Class="FlickrDemo.Views.ImageMapPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FlickrDemo.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Maps="using:Windows.UI.Xaml.Controls.Maps"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ProgressBar x:FieldModifier="public"  x:Name="progressBar" Height="15" Margin="0 5 0 0"
                        VerticalAlignment="Center" IsIndeterminate="True"  Visibility="Visible" Canvas.ZIndex="100"/>
        <Maps:MapControl x:Name="map" LoadingStatusChanged="map_LoadingStatusChanged" />

    </Grid>
</Page>
